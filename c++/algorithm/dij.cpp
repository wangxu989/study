#include<iostream>
using namespace std;
int main() {
  while(n) {
    int min=INT_MAX,flag = 0;
    for(int i = 0;i < n;i++) {
      if(flag(i)) {
        if(dis[i] < min) {
          min = dis[i];
          flag = i;
        }
      }
    }
    for(int i = 0;i < path[flag].size();i++) {
      dis[]
    }


  }

}
